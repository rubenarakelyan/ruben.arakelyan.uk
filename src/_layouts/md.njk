{% extends "./base.njk" %}

{% block content %}
<section class="content">
  <section class="page-heading">
    <h1>{{ title }}</h1>
  </section>
  <section>
    {{ content | safe }}
  </section>
</section>
{% endblock %}
